import { describe, it, expect } from "vitest";
import { WebCallButton } from "./WebCallButton";

describe("WebCallButton", () => {
  it("is defined", () => {
    expect(WebCallButton).toBeDefined();
  });
});
