import { describe, it, expect } from 'vitest';
import { StorageFileBrowser } from './storage-file-browser';

describe('StorageFileBrowser', () => {
  it('is defined', () => {
    expect(StorageFileBrowser).toBeDefined();
  });
});
