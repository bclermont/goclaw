import { describe, it, expect } from 'vitest';
import { StoragePage } from './storage-page';

describe('StoragePage', () => {
  it('is defined', () => {
    expect(StoragePage).toBeDefined();
  });
});
